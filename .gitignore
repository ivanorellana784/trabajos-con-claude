# Salida del runner nocturno: se genera sola y puede describir trabajo interno.
# El repo es publico, asi que no se sube.
bitacora/
informes/

# Dependencias y basura del sistema
node_modules/
Thumbs.db
desktop.ini

# La llave que da VTube Studio al plugin: es de este equipo, no se comparte.
vtube-studio/token.json
